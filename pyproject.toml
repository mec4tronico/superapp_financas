[tool.poetry]
name = "superapp_financas"
version = "0.1.0"
description = "MVP - gerenciador de carteira (FastAPI + SQLite)"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
